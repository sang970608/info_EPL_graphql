type Team{
    id: Int!,
    teamname: String!,
    stadium: String!,
    director: String!,
    players: [Players]
}

type Players{
    backNo: Int!,
    name: String!,
    position: String!,
    captain: Boolean
}

type Query{
    teams: [Team]!
    team(id: Int!): Team
}